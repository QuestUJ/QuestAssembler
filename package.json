{
  "name": "QuestAssembler",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "pnpm --prefix server lint && pnpm --prefix client lint && pnpm --prefix common lint",
    "lint:fix": "pnpm --prefix server lint:fix && pnpm --prefix client lint:fix && pnpm --prefix common lint:fix",
    "format": "pnpm --prefix server format && pnpm --prefix client format && pnpm --prefix common format",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "pnpm": {
    "patchedDependencies": {
      "jose@5.2.4": "patches/jose@5.2.4.patch"
    }
  }
}
