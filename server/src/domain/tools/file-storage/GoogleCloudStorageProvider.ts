import { IFileStorage } from './IFileStorage';

export class GoogleCloudStorageProvider implements IFileStorage {
    uploadImage(image: ReadableStream): string {
        image;
        return '';
    }
}
